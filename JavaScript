/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/Aitools',
  assetPrefix: '/Aitools',
  images: { unoptimized: true },
  trailingSlash: true,
};

module.exports = nextConfig;
